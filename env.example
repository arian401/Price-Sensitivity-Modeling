# .env.example
# Rename this file to `.env` and fill in production credentials.
# NEVER commit the real `.env` to version control.

DB_USER=your_username
DB_PASS=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
